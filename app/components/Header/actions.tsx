import { Moon, Search } from 'lucide-react';

export default function Actions() {
  return (
    <div>
      <button type="button">
        <Search />
      </button>
      <button type="button">
        <Moon />
      </button>
    </div>
  );
}
